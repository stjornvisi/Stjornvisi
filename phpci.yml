build_settings:
    mysql:
        host: "localhost"
        user: "root"
        pass: ""

setup:
    mysql:
        import-from-file:                   # This key name doesn´t matter
            import:
                database: "stjornvisi_test"             # Database name
                file: "assets/db/stjornvisi-empty.sql"      # Relative path in build folder
